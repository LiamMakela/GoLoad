server:
  port: 8080

load_balancer:
  strategy: least_connections

health:
  interval: 3s
  timeout: 2s

proxy:
  timeout: 5s
  retries: 2

backends:
  - url: "http://localhost:8001"
  - url: "http://localhost:8002"

rate_limit:
  enabled: true
  requests_per_second: 1
  burst: 2
